import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

console.log("the couple errors that get thrown don't stop anything from working.");
console.log("the <post> related one especially came out of nowhere, started showing up the day after writing that..");
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <App />
);


